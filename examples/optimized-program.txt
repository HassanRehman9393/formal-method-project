// Optimized version of the program
// Should be equivalent to optimized-vs-unoptimized.txt

// Initialize variables
x := 5;
y := 10;

// Optimized code with constants folded and redundancy removed
z := x + y;  // Directly compute x + y

// Compute x + y only once
a := z;      // Reuse z instead of computing x + y again
b := z;      // Reuse z again

// Final result - should be the same as the unoptimized version
result := z + a + b; 